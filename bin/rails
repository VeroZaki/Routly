#!/usr/bin/env ruby
# Minimal binstub. Prefer: bundle exec rake test

case ARGV.first
when "test"
  exec "bundle", "exec", "rake", "test", *ARGV.drop(1)
else
  puts "Usage: rails test"
  puts ""
  puts "This is a minimal Rails app. Use: bundle exec rake test"
  exit 1
end
